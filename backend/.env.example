# Optional. When set, all API requests require this header.
# ACCESS_PASSWORD=

# Comma-separated frontend origins (defaults include bouncepass.net)
# ALLOWED_ORIGINS=https://bouncepass.net,https://www.bouncepass.net,http://localhost:3000
